experiment: diffusion1d
tag: baseline
seed: 0

model:
  hidden: [64, 64, 64]
  activation: tanh

grid:
  n_x: 128
  n_t: 64
  x_min: 0.0
  x_max: 1.0
  t_min: 0.0
  t_max: 1.0

optim:
  lr: 0.002
  epochs: 200
  batch: 4096

physics:
  alpha: 0.1

io:
  results_dir: results
  save_ckpt: true
