# ===================================================================
# physical-ai-stl — repository .gitignore
# Goal: keep commits lean, reproducible, and CI-friendly while avoiding
#       accidental commits of large data, experiment artifacts, secrets,
#       or local editor/system files.
# ===================================================================

########################
# 1) Python essentials #
########################

# Byte-compiled / cache
__pycache__/
*.py[cod]
*$py.class

# C extensions / Cython debug (rare here, but safe to exclude)
*.so
cython_debug/

# Test / coverage / tool caches
.pytest_cache/
.coverage
.coverage.*
coverage.xml
.cache/
htmlcov/
.tox/
.nox/
.hypothesis/
.pyre/
.pytype/
.mypy_cache/
.pyright/
.ruff_cache/
.pylint.d/
.benchmarks/          # pytest-benchmark

################################
# 2) Packaging & build outputs #
################################
build/
dist/
pip-wheel-metadata/
*.egg-info/
.eggs/
wheels/
share/python-wheels/
*.egg
MANIFEST
# PyInstaller
*.manifest
*.spec
# Installer logs
pip-log.txt
pip-delete-this-directory.txt

#####################################
# 3) Virtual envs & local env files #
#####################################

# Virtual environments (prefer project-local .venv for speed)
.venv/
venv/
env/
ENV/
__pypackages__/
.conda/
condaenv.*/

# Python version managers
.python-version        # pyenv

# dotenv/direnv secrets (keep examples only)
.env
.env.*
!.env.example
!.env.sample
!*.env.example
!*.env.sample
.envrc
.direnv/

########################################################
# 4) Project artifacts (root-anchored, ML-specific)    #
#    These are created by scripts/Makefile and common  #
#    ML tooling; they often contain large binaries.    #
########################################################
/results/
/figs/
/logs/
/outputs/            # Hydra single-run default
/multirun/           # Hydra multi-run sweeps
/runs/               # TensorBoard / torch.utils.tensorboard
/wandb/              # Weights & Biases (local run dir)
/mlruns/             # MLflow local tracking dir
/lightning_logs/     # PyTorch Lightning default log dir
/tensorboard/        # Optional TB export dir
/ray_results/        # Ray Tune sweeps (if used)

# Experiment artifacts and checkpoints
/artifacts/
/checkpoints/
/models/

# Data staging (retain tiny samples or metadata via README)
# If you must commit a small sample, place a README alongside it.
/data/
/datasets/
!/data/README*
!/data/*/README*
!/datasets/README*
!/datasets/*/README*

# Temporary & cache directories
/tmp/
temp/
tmp/
cache/
.caches/
scratch/

# Cluster / job runners
slurm-*.out
nohup.out

#################################
# 5) Editors, OS & misc clutter #
#################################

# macOS / Windows
.DS_Store
.DS_Store?
.AppleDouble
.LSOverride
Icon?
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
Desktop.ini

# IDEs / editors
# Keep recommended workspace settings if explicitly provided
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
*.sublime-*
.vim/
*.sw?
*~
# Emacs backups/locks
\#*\#
.\#*

#############################
# 6) Docs & site generators #
#############################

# Sphinx / MkDocs outputs
docs/_build/
site/

# Jupyter
.ipynb_checkpoints/
.ipython/
.jupyter/
