# Optional heavy dependencies for experiments and monitoring.
# Install with: pip install -r requirements-extra.txt
rtamt>=0.3.5
moonlight>=0.3.1
spatial-spec>=0.1.1; platform_system != "Windows"
neuromancer>=1.5.4
torchphysics>=1.0.4
pytorch-lightning>=2.0,<3.0
nvidia-physicsnemo>=1.2; platform_system == "Linux"
matplotlib>=3.8
tqdm>=4.66
pyyaml>=6.0.2
scipy>=1.11

# --- Optional convenience (comment in if you need them) ---
# install-jdk>=1.2           # Bootstrap a JDK from Python before using MoonLight (Java 21+)
# warp-lang>=1.5             # For PhysicsNeMo’s Darcy demo (not required for this repo)
